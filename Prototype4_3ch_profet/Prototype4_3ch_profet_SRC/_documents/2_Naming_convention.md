# Naming Convention

To separate the code made for the Ctrl MC system from the STM32 HAL code, all files, variables and methods uses `cmc_` prefix. 

The `snake_case` style is used for all files, variables and methods.